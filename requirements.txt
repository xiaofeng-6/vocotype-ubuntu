# jieba 等仍用 setup.py 装包时，若见 canonicalize_version(strip_trailing_zero=) 错误，需较新的 packaging
packaging>=24.0

sounddevice==0.5.2
keyboard==0.13.5
pyperclip==1.11.0
# Linux/macOS: 剪贴板 Ctrl+V 与 unicode 回退
pynput>=1.7.6
librosa==0.11.0
soundfile==0.13.1
funasr_onnx==0.4.1
jieba==0.42.1

# Volcengine BigASR 流式识别（backend == "volcengine" 时需要）
websockets>=12.0

# Optional backends for model downloading (recommended)
# 选其一或都装：用于 AutoModel 拉取/缓存模型
modelscope==1.30.0
customtkinter>=5.2.2

# 说明：
# - 切换为 ONNX 路线（onnxruntime）；如需 GPU/PyTorch，请安装 torch(+cu) 与 torchaudio。

